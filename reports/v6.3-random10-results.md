# v6.3 同题十题完整结果

严格本地公开集 pass@1：**0/10（0%）**。相比 v6.2 的 1/10 下降 10 个百分点。持续改进 goal 尚未达标；不能将 173 项工程测试通过描述为实际解题能力提升。

## 比较及口径

同一题单、顺序、qwen2.5-coder:14b、18 步、900 秒单题预算、4000 输出 token、360 秒请求超时、300 秒 checker 超时、starter=false，沿用原始公开 checker。每题一个 rollout，内部修复计入该 rollout；没有失败题择优重跑。v6.3 额外要求内部三层验证，不放宽评分。

|版本|同十题严格通过数|
|---|---:|
|v5.6 canonical|4/10|
|v6|1/10|
|v6.1|2/10|
|v6.2|1/10|
|v6.3|0/10|

这是已知公开集上的整体框架回归，不是单组件消融，也不证明隐藏集泛化或任何单项机制的因果效果。

## 逐题结果

checker 列为通过/失败/错误；请求列为请求尝试/完成响应。所有 Agent 退出码与 checker 退出码均为 1，reward 均为 0。

|题目|checker|耗时秒|请求|主要证据|
|---|---:|---:|---:|---|
|geometric-mean-reverting-jd|0/0/34|895.51|14/13|两次最小二乘 SVD 不收敛，随后改成错误输入路径；没有进入审计|
|earnings-surprise-calculator|0/8/0|895.52|14/13|输入路径混用 scratch，修后将字符串当字典；没有进入审计|
|fama-french-factor-model-new|0/101/0|836.82|12/12|compute 未返回非空结果 mapping，三次重复失败；最终源码尾部空 lambda 覆盖前面的函数实现；不是旧版日期长度错误的复现|
|implied-vol-approximations|0/10/0|817.53|18/18|初稿语法错误，反复局部行编辑，无阶段记录|
|localvol-barrier|0/7/32|849.47|18/18|先复制多个已有输入，后续语法修改被拒，无阶段记录|
|pca-factor-portfolio|0/8/0|895.48|15/14|input/scratch/params.json 与 input_path 参数非法，卡在加载|
|dcc-garch-portfolio-var|0/63/0|626.83|18/18|把下载数据脚本复制成求解器；反复复制同内容、运行、记录相同方法计划|
|american-option-fd-new|0/54/0|895.51|15/14|程序曾正常退出但无要求的交付物；后续语法修复失败，无阶段记录|
|variance-swap-replication|0/17/0|893.59|14/14|三次以位置参数调用仅允许关键字参数的 computation，卡在审计接口|
|asian-option-levy-curran|0/0/25|895.05|16/15|compute 签名与阶段协议冲突，data_path 未定义/缺少参数，未进入审计|

记录的 Agent 耗时合计 **8501.33 秒（141.69 分钟）**，平均 850.13 秒。输入 token 1,414,586，输出 token 45,862。**154 次请求尝试，149 次完成响应**，不可把尝试数当作模型实际回答数。

创建时间 2026-09-23 10:49:02 UTC，最终汇总 2026-09-23 23:59:40 UTC，日历跨度约 13 小时 11 分钟，明显长于逐题记录耗时。现存结果不足以确认期间停顿的具体原因，不能把记录耗时当成用户真实等待时间。

## 验证机制采用情况与局限

- 6/10 题有阶段记录，共 18 份：加载失败 4 次、计算失败 11 次、审计失败 3 次；完整阶段通过 0 次。
- 没有生成成功的 three-layer-v1 审计。variance 的 3 次审计记录是接口异常，标为 legacy_or_unrecorded，**不是新验证通过，也不是已证实的金融方法失败**。
- 10 题均未写出要求的最终交付物；所以不是仅因额外内部门槛扣掉了已通过原始 checker 的答案。
- 内部验证器仍有价值，但当前小模型不能稳定使用其协议。假设“提示和协议负担加重导致退化”与数据相容，但不是单组件因果证明。
- 日志证实重复复制同内容和记录同一方法计划被当作 mutated，清空了无进展计数。这是控制器缺陷，不应归咎于金融算法。
- 根因未证实的 SVD、超时等仍是待诊断现象。没有把未经验证的反思提升为跨任务事实。

## 封存核对

只读核对记录：`reports/v6.3-random10-audit.json`，complete=true、audit_consistent=true。

- 10 个唯一 ID 与题单、run_config 顺序一致，results 与 metrics 均显式 starter=false。
- reward、退出码、原始 checker 日志 P/F/E、summary 总和与逐题耗时一致。
- 框架源码 SHA256：`9d122a48dcebf245c9824c0eb908b0c1bea5f041cf16e8c1809495e1ae78a28e`，与 run_config、冻结目录和 tar 一致。
- runner SHA256：`81e1c93069d1e1201c97c6784edb7832d19eeba0d02a04ca4147b2c1013f4b5a`，核对一致。
- manifest 中 58 个文件逐项与冻结目录及 tar 核对一致，两份 manifest 字节一致；经验快照 SHA256 与登记的 `cf241e8c579cf8a38434c9e7a997d4d95b9280052083b4b3b086da8e05d2a00f` 一致。
- 工具 session 78854 返回 exit 0（批次运行器完成，不代表题目通过）；父 PID 6414、防休眠 PID 6481、初始子 PID 6482 均已退出。

不修改或删除本轮结果，不重跑失败题择优。后续在独立 v6.4 开发目录修复具体协议/循环问题，工程验证完成后另行冻结、登记整轮实验；不提前宣称改进有效。

### 后续输入来源检查补充

2026-09-24 检查发现公开数据检出目录中的 DCC prices.csv 不同于 Git HEAD，当前 mtime 早于 v6.2 启动。v6.2/v6.3 未保存完整输入哈希，不能仅凭 mtime 证明跨轮输入逐字节一致。以上 0/10、日志和源码核对仍成立；历史横向比较须保留此来源局限，不能将其说成输入完全受控的消融。v6.4 将保存输入快照并检测运行前后漂移，不修改历史结果。
