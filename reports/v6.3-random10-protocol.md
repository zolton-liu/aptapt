# v6.3 同题十题：待启动实验协议

状态：尚未启动。此文档固定比较口径，不是已完成的源码冻结登记；启动前必须补充精确哈希与运行信息。v6.2 当前批次结束并核对封存前，不启动本实验。

## 预定设置

- 题单仍为 `configs/random10-20260923.txt`，同样十题及原顺序；不得重采样。
- 本地 `qwen2.5-coder:14b`，模型 digest 须与 v6.2 启动登记核对一致。
- temperature=0、seed=0；每题 18 步、900 秒；单次响应最多 4000 tokens、请求超时 360 秒；checker 超时 300 秒；预留 5 秒；上下文估算上限 16000。
- `starter=false`；三层验证严格模式开启；阶段协议、版本化任务内记忆、定位修复上下文开启。
- 经验仅使用启动前已验证、冻结的日期键对齐规则；不注入原题答案或未经验证的根因。
- 原公开 checker 及原本地挂载路径适配口径不变。严格成功要求 Agent 和 checker 均正常退出且未超时。

## 启动门槛

1. v6.2 十题结果核对完成，进程已经结束，源码和 runner 哈希与登记一致或偏差已披露。
2. 活动候选版完整测试通过；将源码、运行脚本及经验复制到独立冻结版本目录，并保存快照。不得直接从仍在修改的活动目录启动。
3. 记录源码聚合哈希、runner 及其公共辅助脚本哈希、经验内容哈希、题单哈希、模型 digest、实际命令和启动时间。
4. 使用从未存在的新实验目录；拒绝覆盖、自动 resume 或重新执行留有轨迹但无结果的题。

## 报告要求

完整十题一个分母，报告逐题通过/失败/错误、严格 pass@1、持续时间、模型请求尝试与已完成响应。无法确认的请求数记为未知，不拿成功响应数冒充尝试数。

报告三层验证采用率及失败阶段，区分内置独立重算、模型自写参考路径及旧版执行。缺失验证不能称为通过。

对比同题 v6.2，以及历史 v5.6 的 4/10、v6 的 1/10、v6.1 的 2/10。当前是框架整体回归，不是单组件消融；单次十题成绩差异不能证明统计显著，也不代表官方隐藏集泛化。若超过 v6.2 但未超过 v5.6，应明确说明仍低于历史最佳。

若中断，保留所有记录，不能为了得到更好结果重跑有轨迹的题。若完整一轮未提升，先检查失败证据，下一次改动和完整实验另行编号登记，保留全部历史轮次，披露对已知公开集的适配风险。
