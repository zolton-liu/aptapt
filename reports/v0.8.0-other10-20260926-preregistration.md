# v0.8.0 其他十题预登记

日期：2026-09-26

实验：`workflow-v0.8.0-other10-20260926-coder14b`

状态：本文写入时尚未启动。

## 目的与冻结版本

本实验只测量已上传 v0.8.0 框架在另一组公开任务上的单次 rollout 表现，不在
运行中修改 Agent、模型、预算、题单或 checker，也不把失败题重跑后择优。

- 上传代码提交：`cd6e95b8ac7bde3b890848feadaa829f04911199`
- 上传准备检查点：`checkpoint/v6.8-upload-ready-20260925`
- CodaBench 回执检查点：
  `checkpoint/v0.8.0-codabench-945527-20260926`
- CodaBench 提交 ID：`945527`
- 开发分支：`dev/local-verifier`
- 启动提交由 `scripts/dev_local.py` 在运行 provenance 中固定；相对于上传代码，
  预期只有报告、回执和本次预登记题单发生变化，Agent 源码不变。

## 题单抽样

从 87 道公开题中先排除 `configs/random10-20260923.txt` 的原固定十题，再对
剩余 77 个按字典序排列的 ID 使用
`random.Random(20260926).sample(pool, 10)`。抽中的顺序为：

1. `t1-bs-greeks-pde`
2. `t1-binance-btc-participation-tca`
3. `t1-alpha-hedge-strategy`
4. `t1-etf-overlap-redemption-pressure`
5. `t1-sma-crossover-spy`
6. `t1-cta-basel-capital`
7. `t1-barrier-garch-var`
8. `t1-copula-equity-fitting`
9. `t1-mc-greek-surface-1`
10. `t1-sentiment-factor-alpha`

冻结题单：`configs/random10-unseen-20260926.txt`，SHA-256：
`f9d1d064b27b33545d379f3f8137cbf97009dea639dd2c15b1c298eb65f252f7`。
抽样记录：`reports/random10-unseen-20260926-sampling.json`。

“其他十题”仅表示不与原固定十题重合。仓库保留了历史公开集实验，其中个别题
可能曾在更早的小规模工程实验中出现，因此这不是严格盲测，更不是官方隐藏集。

## 固定运行条件

- 本地模型：`qwen2.5-coder:14b`
- 模型 digest：
  `9ec8897f747e246e970bc5cfdda85d22f1123dc2e3d34978a010a75968716849`
- profile：`configs/local-dev.toml`，SHA-256：
  `7efc557ef0f378a84a51b715bb563ff6139077a9ea7c7911a3b22744491a55ed`
- temperature 0、seed 0；每题最多 18 个模型步骤和 4,000 response tokens。
- 单次模型请求 360 秒、每题 900 秒、checker 300 秒。
- starter 禁用；上下文记忆、修复上下文、验证经验和合法运行自动完成启用。
- 使用原公开 checker；不改变官方 House adapter 默认值或已提交镜像。

## 计分与完成条件

严格 pass@1 为十题中首次且唯一一次运行通过公开 checker 的题数除以 10。
工程测试和连接检查不计 pass@1。完成后核对题目顺序、唯一 ID、
`starter_used=false`、原 checker 日志、reward/退出码、summary、源码/输入快照、
模型 digest、profile、题单和 provenance。若进程中断，保留已有轨迹和结果，
不自动恢复或补跑。

计划结果报告：`reports/v0.8.0-other10-20260926-results.md`。
