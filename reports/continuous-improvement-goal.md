# 持续改进 goal

2026-09-23 用户明确要求开启；状态 active，未设置 token 预算。

## 目标和验收

保持现有 v6.2 同题十题实验不变，在隔离目录改进验证、失败诊断和经过验证的经验。待完整基线封存后，以同十题、同模型、同预算、starter=false、原始公开 checker 进行预登记的新实验，争取取得高于 v6.2 的严格 pass@1，并逐题解释差异。

同时必须报告同题历史 v5.6 的 4/10：仅超过一个退化基线，不能描述成超过历史最佳。所有结果限于已知公开集，不声称隐藏集泛化。内部修复属于单次 rollout 的预算；禁止失败题重跑择优、删分母、放宽 checker 或更改题目要求。没有要求推送 GitHub。

## 当前工作位置

- 运行中的基线：`eval_runs/workflow-v6.2-contract-pipeline-random10-20260923-coder14b`，父进程 92783。
- 活动候选源码：`work/verifier-v6.3.SfBPS3`。
- `reports/framework-v6.3-isolated.tar.gz` 是开启 goal 前的 157 测试版快照，不代表后续活动目录的最新内容；在新评测启动前生成独立、不可混淆的冻结快照。
- 基线尚未结束，不合并主目录源码、不并发启动下一轮模型评测。现有 heartbeat 负责 v6.2 完成检查，goal 负责持续研发和随后完整实验；两者都不得启动重复批次。

## 本轮新增加固

1. 损坏但语法有效的经验 JSON（如 null、数组、validation=null）安全返回无可用经验，不使求解崩溃。
2. 经验晋升禁止在关闭断言的 Python 优化模式中运行；验证记录额外绑定晋升引擎源码哈希，规则代码或引擎变动均需要重新验证。
3. 经验检索的内容与登记哈希基于同一次读取，避免二次读取产生不一致。
4. 真实输出 JSON 递归检查非有限数值，覆盖 `1e309` 指数溢出及嵌套 NaN，而非仅识别字面量 NaN。
5. 补充 CSV 列序、行数、非有限单元格与不齐行测试。

新增治理测试与验证学习测试共 23 项通过；完整回归 **162 项通过（30.85 秒）**。经验目录已按新晋升引擎重新执行五个合成验证用例，仍不声明原始 Fama–French 根因已经证实。

## 后续执行顺序

1. 检查候选版的上下文开销和任务类型适配，避免验证协议本身挤占小模型实现预算。
2. 基线完成后核对十题、源码和 checker 记录，生成其独立最终报告。
3. 冻结候选源码、运行器、经验目录、题单、模型及参数，登记实验，再启动一次完整十题评测。
4. 对比严格成绩、checker 细项、协议采用率、错误阶段、请求尝试与实际响应；不把工程测试当作 pass@1。
5. 若未改善，依据证据提出下一轮改动；每轮另存完整结果，记录对已知公开题的适配风险。

## 第二轮工程检查

- 修复严格模式提示与门槛矛盾：带模板、预置算子和软件修复类任务现在也得到阶段验证协议；保留其原实现，不允许以此豁免审计。
- 按任务类型选择详细验证 API 文档，通用参考路径与混合任务的全部函数可用性保留。87 题初始上下文预检查全部在预算内，最高从 13029 降至 12561（字节启发式估算，不是实际模型 token）。
- 严格验证与禁用阶段协议的冲突配置在调用模型前报错，避免消耗整轮请求才发现无法完成。
- 新版 runner 原子创建单题目录，不再删除已有目录；即使有轨迹而无结果也拒绝自动覆盖。结果另外分列请求尝试与实际响应，缺失尝试数记为未知。
- 定价检查中的维度、网格配置和无效容差错误走实现修复，真实细化差异不满足条件才保留方法复核建议，避免把验证接口使用错误误当作方法失败。
- 待启动比较协议见 `reports/v6.3-random10-protocol.md`；尚未启动下一批模型评测。
- 本轮最终完整测试：**170 项通过，31.03 秒**。工程测试不替代新的 pass@1 实验；goal 仍 active，未满足完成条件。

## 候选版冻结

完整控制循环新增三项合成测试通过：真实运行后的严格完成、失败结果拒绝完成、修改源码后保留旧失败并重新验证。候选已复制到 `work/releases/v6.3-verifier.Jgg53G`；从该冻结目录运行完整测试 **173 项通过，35.39 秒**，运行器命令入口检查正常。

冻结清单和快照见 `reports/v6.3-random10-preregistration.md`。不要再修改此冻结版本；如发现必须修复的问题，新建版本并重新登记。v6.2 仍在运行，当前没有启动改进版模型评测，goal 仍未完成。

## 只读成绩核对准备

新增独立工具 `work/audits/audit_local_experiment.py`，不导入或修改运行中的 Agent，也不启动模型或 checker。核对题单顺序、唯一 ID、显式 starter=false、退出码与 reward、pytest 原日志计数、汇总、逐题耗时、源码/runner 与 tar 快照哈希，并汇总阶段证据。请求尝试来自 run_metrics，成功响应来自 trajectory，分别统计。

六项独立合成测试通过，证明能识别重复结果、源码漂移及 checker 计数不一致；未完成批次的 `strict_pass_at_1` 保持 null。当前五条落盘结果的只读核对一致，不能据此报告最终十题成绩。此工具位于冻结版本之外；候选框架仍保持 173 项测试的已登记状态。

## 已进入比较评测

v6.2 最终 **1/10（10%）**，完整核对一致，详细报告 `reports/v6.2-random10-20260923-results.md`。131 次请求尝试、127 次实际响应；4 题有阶段记录但无完整阶段通过，唯一严格通过题为 legacy Asian option。原父进程及防休眠进程已退出，原 heartbeat 已 PAUSED。

v6.3 于北京时间 **2026-09-23 18:49:02** 从冻结目录启动，实验 `workflow-v6.3-verifier-memory-random10-20260923-coder14b`，session **78854**、父 PID **6414**、防休眠 PID **6481**。模型 digest、题单、58 个文件及预算核对一致，详见预登记补录。

本阶段只监测该已启动实验；不再修改冻结目录、不启动重复运行、不在失败题上择优重跑。终态后完成对比审计，至少需要严格成绩高于基线 1/10 才满足 goal 的量化部分；即便如此，也必须披露是否仍低于历史 4/10。goal 仍 ACTIVE，尚无 v6.3 最终成绩。

## v6.3 已结束，进入 v6.4 独立改进

上述运行状态为历史记录。当前 v6.3 已完成 10/10 题，严格成绩 **0/10**，未超过 v6.2；session 78854 已 exit 0，父进程及防休眠进程均退出。不要再次轮询旧 session 或重启该批次。

报告：`reports/v6.3-random10-results.md`，只读核对：`reports/v6.3-random10-audit.json`。题单、退出码、原始 checker、汇总、源码/runner/tar/58 文件 manifest 与经验哈希一致。154 次请求尝试、149 次实际响应，6 题有阶段记录、0 题完成三层审计。

当前活动源码是 **`work/verifier-v6.4.CZWn5T`**（不是 v6.3 开发目录），未冻结、未启动下一轮评测。已修复真实字节变更判断、重复方法计划、失败编辑按版本绑定、阶段签名提示、输入路径证据等；完整 **183 项测试通过（32.20 秒）**，87 题初始上下文预算检查通过。详细改动与下一步见 `reports/framework-v6.4-progress.md`。

下一步应处理实际阶段绑定位置的诊断及旧写文件算子与阶段协议兼容性，然后冻结、登记整轮 v6.4。同题结果不足的 goal 保持 ACTIVE；不得将工程测试通过或省下调用数作为完成条件。无实验正在运行，无需 verified wait。

## v6.4 已冻结并启动（最新状态）

实际绑定函数定位和旧 writer 的先暂存、审计后发布适配已完成，并保留三层审计门槛。新增输入/检查器快照和漂移检测；DCC 原数据早已有本地修改，历史轮未保存完整输入哈希，局限见 v6.4 预登记，不能声称历史输入逐字节已证实一致。

冻结目录 **work/releases/v6.4-evidence.L189Io**；完整测试 **197 passed in 33.51s**。框架 SHA256 `a4798b856bb83e6d01ce587ef5e46e14d6e280726ea15f7c87eba731c1322831`。源码 manifest、tar、经验和输入清单均已登记，报告 `reports/v6.4-random10-preregistration.md`。

北京时间 **2026-09-24 10:43:19** 启动 `eval_runs/workflow-v6.4-evidence-adapter-random10-20260924-coder14b`；session **20883**，评测父 PID **15045**，防休眠 PID **15112**，初始 Agent PID **15127**。参数同前，增加 --snapshot-inputs；input-manifest SHA256 `c57e85087d1e15f6d8785c330d6e3713b09fcebe40948090c4c5898ff55289ca` 已核对一致。

下一步只检查这批现有实验；不要重跑失败题、不更改冻结源码/输入/评分，不混入旧轮数据。结束后使用 work/audits/audit_local_experiment.py 指向本冻结目录审计；该只读工具已经增加输入清单与快照核对，7 个独立测试通过。还要重新核对模型、61 文件 release manifest、tar 与经验哈希，确认防休眠释放，报告协议采用、通过数和错误类型。goal 仍 ACTIVE，成绩待定；无须新增 heartbeat。

### 运行环境观察

2026-09-24 11:07–11:09 检查确认 session 20883 与 PID 15045/15127/15112 仍存活，首题有 3 次实际响应、进入第 4 次请求。日历经过约 24 分钟，但 trajectory 活跃耗时约 288 秒。pmset 日志显示多次 Maintenance Sleep/DarkWake，另有一次 Dark Wake Thermal Emergency。详见 `reports/v6.4-runtime-observation.md`；已提醒用户唤醒电脑并确保散热，不强行绕过系统保护。

这不是已退出或需要重新启动的证据；仍跟踪原 session，保留所有结果。没有修改预算或电源配置。前一轮为冻结启动与工程改进（progress）；本轮为确认存活的等待及新增环境原因证据（verified wait + diagnostic progress）。

## v6.4 已完成封存（最新状态）

2026-09-24 13:59:38 北京时间十题全部结束，严格 **0/10**。报告 `reports/v6.4-random10-results.md`，审计 `reports/v6.4-random10-audit.json` 完整一致。61 个源码清单文件与 tar、76 个输入/checker 快照、模型 digest、题单顺序、starter=false、原始 checker 及汇总均核对无偏差。session 20883 exit 0，PID 15045/15112 均已退出；不要再轮询这些旧句柄。109 次请求尝试、106 次完整响应，8 题有阶段回执，0 份有效三层报告、0 次阶段完整验收。

下一步不需要等待。goal 保持 ACTIVE，量化目标没有达到。依据 `reports/v6.4-midrun-failure-evidence.md` 中已复现的日志文件名污染分类问题及接口/修复循环证据，建立新的 v6.5 隔离开发目录；保留严格验证要求，先验证工程修复，再冻结登记完整十题比较。不得修改任何已封存轮次或择优补跑失败题。

## v6.5 活动开发目录

已建立 **work/verifier-v6.5.DWdd1o**。完成终止异常优先的错误分类、AttributeError/IndexError 实现路由、审计多问题反馈与 API 契约提示；全套 **234 passed in 33.91s**。尚未冻结或启动新模型批次。详情 `reports/framework-v6.5-progress.md`；下一步是可靠的函数级局部编辑及控制循环测试，再做最终预检查和登记。此轮为实际代码与测试进展，不是等待，仍未满足 goal。
