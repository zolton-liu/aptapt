# v6.5 隔离改进进度

活动目录：`work/verifier-v6.5.DWdd1o`，从封存的 v6.4 复制。未修改主目录脏源码或任何已完成实验；尚未冻结、尚未启动 v6.5 模型评测。goal ACTIVE，pass@1 提升目标仍未达到。

## 已完成并测试

1. 错误分类以结构化失败阶段和终止异常为主，不再把 traceback 路径、源码标识符、先前 warning 当成当前失败原因。修复 `finance_ops.py` 的 `nan` 子串误判；AttributeError 明确归入实现类型错误，IndexError 归入维度/索引错误，未知异常保守处理。数值异常仍不等于方法根因已经证实；原始日志完整保留。
2. 审计协议一次反馈重复名称、缺失层及已执行失败检查证据。严格要求不变：缺交叉检查、bool 伪造声明、失败数值检查仍拒绝完成。新 AuditProtocolError 明确走实现修复。
3. 提示补充 `structure` 要求字典、多个结构检查应显式命名、AuditReport 接收一个列表而不是三份位置参数的契约。

最小复现及文件名变体、warning/异常混合、链式异常、真正数值故障、三层门槛和控制器路由均增加回归测试。全套 **234 passed in 33.91s**；这只是工程证据，不是 pass@1 成绩。

## 尚需完成

- 解决完整轨迹中反复发生的局部修复失败。候选方向是按唯一函数名整体替换的语法感知编辑，保留源码版本约束、不可修改输入、拒绝歧义、无效提案不损坏原文件、改动后重新运行与审计；不替模型写金融算法，不自动放宽断言。
- 完整控制循环与变体回归，检查新增工具不会绕开失败版本、工作记忆失效与完成门槛；避免仅增加提示长度。
- 重新检查上下文，再冻结源码/runner/经验/输入/题单/模型与预算，登记完整同十题的新批次。没有授权 GitHub 推送。
