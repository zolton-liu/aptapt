# v0.8.1 全量 87 题本地公开集预登记

日期：2026-09-27

实验：`workflow-v0.8.1-all87-20260927-coder14b`

## 目的与范围

在 v0.8.0 CodaBench checkpoint 上应用 House endpoint thinking 修复后，对本地
Track 1 公开仓库中的全部 87 个任务执行一次固定列表、每题单次 rollout 的评测。
这是已知公开集本地结果，不是官方 House 模型或隐藏集成绩。公开比赛阶段摘要只有
86 个单位；本地仓库额外包含 `t1-polars-api-migration`，因此本轮按用户要求使用本地
完整 87 题作为分母。

## 冻结项

- 源码提交：以启动时 `git rev-parse HEAD` 为准；必须是干净工作树。
- 分支：`codex/checkpoint-v0.8.0-codabench-945527`。
- 题单：`configs/all87-20260927.txt`，按任务 ID 排序，共 87 个唯一 ID。
- 模型：`qwen2.5-coder:14b`。
- 模型 digest：
  `9ec8897f747e246e970bc5cfdda85d22f1123dc2e3d34978a010a75968716849`。
- profile：`configs/local-dev.toml`；starter 禁用；temperature 0、seed 0。
- 最多 18 个模型请求、每次最多 4,000 tokens、模型超时 360 秒、单题外层
  900 秒、checker 300 秒。
- 使用 `.venv-eval/bin/python` 启动；启动前确认 pytest 8.4.2 可导入，避免此前
  系统 Python 导致的 checker `No module named pytest`。
- runner 冻结 Git 源码快照、题单、输入和 checker；旧实验不覆盖、不 resume，
  失败题不补跑、不择优。

## 报告口径

严格 pass@1 为 87 个任务中 Agent 与原始公开 checker 均成功退出的任务数除以 87。
工程测试、模型连通性和内部输出契约检查不计 pass@1。完成后核对 87 个唯一 ID、
题单顺序、`starter_used=false`、原始 checker 日志、reward/退出码、summary、请求尝试
与完整响应、源码与输入哈希以及 provenance。若运行中断，保留所有已有轨迹且不自动
补跑。

计划结果报告：`reports/v0.8.1-all87-20260927-results.md`。
