# v6.7.2 fixed-ten evaluation preregistration

Date: 2026-09-24

Proposed experiment:
`workflow-v6.7.2-staging-fix-random10-20260924-coder14b`

Status: prepared but **not launched**. Although the user authorized v6.7.1, that
attempt ended incomplete during local checker staging. Repository policy forbids
automatically rerunning an incomplete attempt, so v6.7.2 requires renewed user
authorization.

## Frozen inputs and runtime

- Task list: `configs/random10-20260923.txt`, exactly ten unique tasks in the
  original order; SHA-256
  `84468af7693645c760e6ca46f055aac4a9cf95c0ce3c26ea988e6d58079b6a5a`.
- Branch: `dev/local-verifier`.
- Implementation commit before this preregistration:
  `eea1cf43004c2fb2321b5362c839d8183e4479c0`.
- Model: local `qwen2.5-coder:14b`, digest
  `9ec8897f747e246e970bc5cfdda85d22f1123dc2e3d34978a010a75968716849`.
- Temperature 0, seed 0, 18 model steps, 4,000 output tokens per request,
  360-second request timeout, 900-second task timeout, and 300-second public
  checker timeout.
- Starters disabled. Context memory, repair context, verified experiences, local
  compatibility mode, and valid-run auto-completion enabled.
- Official House defaults and the submitted image remain unchanged.

The local launcher must export a clean committed source snapshot, freeze all ten
inputs as read-only files, reject `fetch_data.py` as a solver template, stage a
private writable copy of checker Python files for local mount-path substitution,
and verify the original frozen input manifest after each Agent attempt.

## Scoring and exclusions

The complete v6.6 rerun2 baseline is 2/10. Success requires at least 6/10 on all
ten tasks, with one fresh rollout per task and no selection or failure-only
rerun. A pass requires Agent completion plus the unchanged public checker.

The interrupted v6.7 experiment (six rows plus a partial DCC attempt) and v6.7.1
(one unscored Agent attempt, zero result rows) are excluded entirely. Their
outputs cannot be copied, completed, or counted in v6.7.2.

No manual output repair, checker edit, task-list change, model change, budget
change, or merging of experiment rows is permitted. This is a known-public-set
local regression, not an official House or hidden-set estimate.

## Completion audit

Completion requires ten unique result rows in frozen order, all
`starter_used=false`, unchanged input manifests, matching Agent/checker exit
codes and rewards, reconciled raw checker logs and summary totals, request versus
response counts, and matching Git/source, model, configuration, runner, and
task-list hashes.

Any interruption is preserved and not resumed automatically. Planned report:
`reports/v6.7.2-random10-20260924-results.md`.

## No-model runner preflight

After preregistration, all ten frozen tasks were exercised through the local
runner's infrastructure-only path: create a temporary read-only input snapshot,
stage its private checker copy, rewrite local mount paths, compile the staged
checker source, and verify the original snapshot against its manifest. All ten
staged successfully, every private checker copy had owner-write permission, and
all ten source snapshots remained unchanged. This preflight made no model call,
generated no participant output, ran no checker assertion, and is not pass@1.
