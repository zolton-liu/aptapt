# v6.2 同题 10 题评测：启动前登记

2026-09-23，按用户要求重跑同样 10 个任务。实验名：`workflow-v6.2-contract-pipeline-random10-20260923-coder14b`。

- 题单：`configs/random10-20260923.txt`，全部 10 题、保持原顺序，不重新抽样；与 v6.1 run_config 的 task_ids 完全一致。
- 题单 SHA-256：`84468af7693645c760e6ca46f055aac4a9cf95c0ce3c26ea988e6d58079b6a5a`。
- 模型：本地 `qwen2.5-coder:14b`；启动前服务确认 digest 为 `9ec8897f747e246e970bc5cfdda85d22f1123dc2e3d34978a010a75968716849`。
- temperature=0，生成 seed=0；18 步，每次最多 4000 输出 tokens，模型请求上限 360 秒，每题上限 900 秒，verifier 上限 300 秒，收尾预留 5 秒。
- starter 关闭；上下文/记忆开启，估算预算 16000；三个 v6.2 特性开关全部开启。
- 框架源码 SHA-256：`f4594772af7b7bf31b2de732cf4e3f7aaeffd7ba8b39cfa0ba1c68cf97b2b86f`。
- runner SHA-256：`4bacda5cd7d6f54bdf99e96e3a1115d8dcdf0e61e3b60bfd508409b4fc75db12`。
- 本地执行解释器：项目 `.venv-eval/bin/python`，不改依赖。旧实验配置未完整保存环境依赖版本，不能宣称系统环境完全受控。
- 启动前测试为 139 passed；本次不把测试结果当作模型成绩。

## 计分与比较

每题一个全新 Agent rollout，内部修复受上述预算约束。严格成功要求 Agent 与官方公开 checker 均正常退出；不重跑失败题择优，不删失败分母，不更改 checker，不覆盖旧实验。

比较同题 v5.6 的 4/10、v6 的 1/10、v6.1 的 2/10。除严格 pass@1，还记录 checker 明细、耗时/调用量、阶段协议采用情况、失败阶段与主要修复问题。此轮是整体 v6.2 回归，**不是单组件消融**；本地已知公开集不等于官方隐藏集或未见题泛化成绩。

## 运行约束

运行期间冻结源码、模型、题单与预算。启动时对当前批次临时抑制空闲休眠，进程结束自动释放，不更改永久电源设置；合盖等仍可能造成休眠。

结果写入独立实验目录。若意外退出，保留现有轨迹，不自动重启或覆盖有轨迹无结果的任务。完成后核对 10 个唯一 ID、顺序、starter=false、源码哈希、逐题 checker 与汇总一致，再汇报最终分数。

最终报告计划写入 `reports/v6.2-random10-20260923-results.md`。当前文档不包含任何预测分数。

## 已启动确认

- 开始时间：2026-09-23 16:31:20（北京时间）。评测父 PID 92783，初始 Agent 子 PID 92785，工具会话 3405；防空闲休眠 PID 92784 随该批次运行。
- 第一题已收到模型响应并进入下一步，确认不是仅创建了配置文件。
- 当前 run_config 的 10 题、预算、三个特性开关及源码/runner 哈希与登记一致。
- 源码快照保存于实验目录 `source-snapshot.tar.gz`，逐个 `.py` 内容核对与运行哈希相符。macOS tar 同时包含 `._*` 元数据条目；复核时只计算原始 `src/qfa_agent/*.py` 文件，不把这些附属元数据算入源码哈希。
- 已将现有完成检查提醒 `10-pass-1` 更新为本次 v6.2 实验，正常运行时保持安静；只在完成、中断或需用户处理时通知，并在终态关闭提醒。
