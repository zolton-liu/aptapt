# v6.4 已完成题目的中途错误证据（非成绩结论）

本记录只检查已完成的 implied-vol 轨迹、阶段回执、工作记忆及冻结源码，不修改运行中源码、题目、预算、输出或评分。当前批次尚未完成；不据此报告最终 pass@1，也不重跑已失败题。

## 已复现：文件名污染错误分类

冻结源码 `work/releases/v6.4-evidence.L189Io/src/qfa_agent/workflow.py` 的 `classify_failure` 对完整错误文本做无边界子串匹配，`nan` 被当作数值异常证据。`finance_ops.py` 的 `finance` 本身包含 `nan`，因此仅加入文件名即可改变分类。

在冻结模块上执行纯分类函数（未请求模型、未运行任何评测题）得到：

| 输入 | 实际分类 |
| --- | --- |
| `AttributeError: int object has no attribute strip` | `unknown` |
| 同一错误，前面增加 `File finance_ops.py, line 39` | `numeric` |
| `ValueError: audit check names must be unique` | `type` |
| 同一错误，前面增加 `File finance_ops.py, line 39` | `numeric` |
| `RuntimeWarning: invalid value encountered in arccos` 后接上述重复名称错误 | `type` |

真实 implied-vol 工作记忆中，`option_type.strip()` 对整数执行导致的 `AttributeError` 也被记为 `numeric`。这证明分类存在误报，不证明该题所有失败都由此导致，也不证明修复后会通过 checker。

后续待实现并验证：优先读取结构化阶段/异常证据；以实际终止异常为主，警告单独保留；为 NaN/Inf 等使用有边界的语义匹配，不从路径、源码标识符推断方法失败；补全 AttributeError 的实现失败分支。测试应覆盖路径改名不改变分类、警告与终止错误混合、真正非有限/收敛失败以及未知异常保守处理。

## 验证接口与编辑失败证据

该题最终 `audit` 构造了两个默认同名 `structure` 检查，缺少独立交叉检查；同时将列表传入要求字典的 `structure`。`AuditReport` 首先抛出检查名称重复错误，尚未形成有效的三层报告，不能称为三层检查已执行通过。

随后轨迹记录了括号不匹配、缩进错误、行范围越界。冻结编辑器使用一基含尾行范围，并在原文件可编译时拒绝破坏语法的提案；工作记忆明确标记被拒绝编辑未改变源码。暂未找到编辑器行号计算错误的证据，不能把这些问题统称为工具 bug。

后续设计候选：一次性返回审计协议的多项具体错误及合法调用形状，避免只解决名称后才发现缺层；保留严格三层要求，不删除失败检查或用同值比较替代独立验证。任何接口或修复上下文更改都放到下一独立版本，先做变体回归，再重新冻结完整十题评测。
