# v6.1：失败恢复与预算一致性改进

## 最终状态

固定 10 题已于 2026-09-23 15:44（北京时间）全部结束：严格 pass@1 **2/10（20%）**，v6 同题 1/10，v5.6 同题 4/10。外层硬超时降为 0，但不能据此认定任务能力已全面优于旧版。最终 121 项测试通过、源码和 runner 哈希一致。逐题检查、回退项、时间与用量、目标完成审计见 [最终效果报告](recovery-v6.1-random10-results.md)。下方预登记与检查点保留为历史记录。

## 全量评测审计

已重新核对 v5.6 的 87 个唯一 ID，与当前公开题目录完全一致，所有记录的 `starter_used=false`。

| 记录 | 通过 | 未完成 | 外层超时 | Agent 错误 | checker 失败 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原始全量 workflow-v5.6-all87-systemic-coder14b | 11 | 41 | 15 | 7 | 13 |
| 整理后 workflow-v5.6-all87-canonical-coder14b | 11 | 46 | 17 | 0 | 13 |

原始一次全量结果为 **11/87（12.64%）**。整理版替换了 7 条 Agent 错误记录；这 7 题重跑后仍全部失败，因此分子没有增加，但失败类型发生变化。已逐条验证整理版与 merge_provenance 指向的原始记录/替换记录一致。不能将整理版描述为没有重跑的独立一次全量实验。

进一步审计发现原始 7 条错误的退出码均为 -15（SIGTERM），且在 900 秒前终止；不能认定为模型自行报错，原始版也不属于完全无干预的受控实验。旧配置未保存源码哈希或模型 digest。87 题逐项记录、任务族分布和可复现性限制见 [全量审计报告](workflow-v5.6-all87-audit.md)。

v6 的固定随机 10 题为 **1/10（10%）**，旧版同 10 题为 4/10，详见 [已完成评测报告](random10-20260923-results.md)。以下修改针对该退步的运行证据，不写入具体题目的答案。

## 已实现修改

| 问题与证据 | 修改 | 验证边界 |
| --- | --- | --- |
| 外层 900 秒终止，但 Agent 依据题卡 1800/2400 秒安排调用 | runner 将同一个时间上限传给 CLI；从入口开始计时，求解器取外层与题卡较小截止时间；模型和 Python/pytest 共用剩余预算 | 不增加每题预算，也不改旧结果；仍保留外层硬超时 |
| 重复读取被告知“已在上下文”，但内容可能被裁剪 | 对成功读取重放真实缓存证据；源码/执行改变工作区后使缓存失效 | 缓存不跳过工作区原有读取限制；连续无修改的重复动作超过 2 次后以未完成结束，避免耗尽全部调用 |
| 历史错误与近期修复相互干扰、上下文不断膨胀 | 在 16000 估算 token 上限内，最多带最近 3 个完整动作—观察组；完整指令与检查点仍固定保留 | 不静默截断任务；完整旧证据保留在轨迹/观察文件中 |
| 同一脚本的旧路径错误与新类型错误同时当作当前故障 | 新失败将同一门槛的旧失败标为 superseded；保留历史约束，只有同门槛成功才标 resolved | 不把源码变化或不同脚本成功当作修复证明 |
| 自动执行嵌套在大观察对象中，截断后错误定位不清 | 单独保留嵌套执行的日志首尾、源码片段和修复提示 | 有限预览仍有大小上限；完整证据可按需读取 |
| 行替换引入缩进错误，破坏原先可解析的脚本 | 在落盘前编译候选编辑；若把可解析脚本变为语法错误，拒绝此次编辑并保留原文件，反馈候选与当前片段 | 只验证语法，不证明金融算法正确；初始坏草稿仍可逐步修复 |
| 已有输出文件可能来自一个最终报错的脚本 | 显式跟踪实际执行失败，finish 与预算结束路径均不能绕过；只有对应脚本成功才清除 | 门槛更严格而非放宽；未执行的缺失路径请求不造成永久失败门槛 |
| Agent 工具被误写成 Python 函数 | 提示明确区分 JSON 工具与 Python 文件写入 API | 真实模型整体评测已完成，但未对这条提示做单独消融，不能归因 |

未引入 RL、模型训练、额外模型、额外采样或跨题答案记忆。

## 自动测试与预检

- 启动前完整测试：**121 passed in 19.28s**；结束后在当前源码复验：**121 passed in 20.25s**。
- 新增 11 个恢复回归测试，覆盖截止时间、执行预算不被无效调用扣除、证据重放与失效、重复动作停止、部分产物不能掩盖报错、不同脚本不能清除失败、历史记忆状态、上下文历史上限、嵌套 traceback 和语法事务编辑。
- 原有一个测试曾把“脚本仍报错，但手工写出文件”视为成功；此次改为断言失败，符合新的严格验收条件。缺失脚本请求仍可正常恢复的原测试保持通过。
- 87/87 题初始上下文可容纳；最大估算 11142 tokens。数据见 [预检结果](recovery-v6.1-context-preflight.json)。
- 测试通过与静态预检不是 pass@1 提升证据。真实模型对照现已完成，完整结果见本文顶部链接。

## 固定对照计划（启动前登记）

- 新实验：`workflow-v6.1-recovery-random10-20260923-coder14b`。
- 沿用 `configs/random10-20260923.txt` 的全部 10 题及原顺序，不重新抽样、不只测试先前失败题。
- 题单 SHA-256：`84468af7693645c760e6ca46f055aac4a9cf95c0ce3c26ea988e6d58079b6a5a`。
- 同一模型 `qwen2.5-coder:14b`，digest `9ec8897f747e246e970bc5cfdda85d22f1123dc2e3d34978a010a75968716849`。
- temperature=0，生成 seed=0，18 步，单次最多 4000 输出 tokens，模型请求超时 360 秒，每题外层 900 秒，verifier 300 秒，结束预留 5 秒；starter 关闭。
- 框架 SHA-256：`f625f0bae8bda96e94830f1dfb282cf1bf99b96a009780007ee51c3464d7dabb`。
- runner SHA-256：`52f7e2c308c8a5ca1e95611713d243cfaa84f5aaae6f5ebe61680b77066f38af`。
- 每题一个全新 rollout，不重跑择优，不修改原 v6 结果。运行期间冻结源码、模型、题单与预算。
- 主要指标仍为严格工作流 pass@1；辅助指标为 checker-only 通过数、外层超时数、Agent 未完成原因、耗时、已记录模型调用与 token 数。
- 比较对象：v6 同 10 题 1/10，以及 v5.6 同 10 题 4/10。优先检验时间预算与重复操作是否改善，再判断成功率；更快失败不等于任务解决能力提升。
- 本轮同时修改多个组件，是框架整体回归，不是单组件因果消融。样本为已知公开集，不能外推官方隐藏集成绩。

## 历史运行检查点（非当前状态）

### 第 9 题检查点（非最终成绩）

已完成 9/10 题，其中 2 题通过，最后一题开始运行。`t1-variance-swap-replication` 为 **0/17**，缺少 `results.json`。执行错误是把浮点值当作嵌套字典读取 `params['filters']['max_relative_spread']`；后续源码版本来回切换被拦截，并触发停滞终止，在 444.54 秒、10 次模型调用后返回未完成。

该题在 v5.6 曾通过，本轮尚未恢复。这证明当前修复仍存在回退，不能把降低超时和更早返回失败表述为全面超越旧版。失败记录未覆盖、未重跑，最终整体结论待第十题完成。

### 第 8 题检查点（非最终成绩）

已完成 8/10 题，其中 2 题通过，第九题开始运行。`t1-american-option-fd-new` 在 728.98 秒、18 次模型调用后未完成，checker **0/54**，缺少全部 7 个交付文件。

实际执行错误为有限差分/PSOR 程序使用尚未赋值的局部变量 `V_old`；后续修补先后产生意外缩进、非法注解目标和循环外 break，均被语法保护拒绝，原文件未被这些错误提案覆盖。保护机制避免了进一步破坏，但模型没有完成有效的局部修复，不能记为解题进步。原始失败保留，未重跑。

### 第 7 题检查点（非最终成绩）

已完成 7/10 题，其中 2 题通过，第八题开始运行。`t1-dcc-garch-portfolio-var` 为 **0/63**，缺少全部 5 个交付文件。轨迹末尾反复出现“原样运行 → 输出校验失败 → 请求 finish 被拒绝”的循环；运行日志只显示保存价格数据，不能证明完成了 GARCH/DCC 和组合 VaR 求解。

本题触发新的停滞终止条件，在 395.50 秒、13 次模型调用后明确返回未完成；未耗尽 900 秒，也未把缺失产物计为通过。这验证了停滞检测在该跨工具循环中的作用，但只减少无效消耗，没有修复任务本身，reward 保留为 0。

### 第 6 题检查点（非最终成绩）

已完成 6/10 题，其中 2 题通过，第七题开始运行。`t1-pca-factor-portfolio` 的 checker **8/8**，Agent 与 verifier 均退出 0，耗时约 170.7 秒，6 次模型调用。这是保留 v5.6 与 v6 原有的通过项，而不是新增成功。前 6 个唯一 ID、顺序、starter=false 和当前框架源码哈希已核对一致。最终 10 题 pass@1 仍待剩余 4 题结束。

### 第 5 题检查点（2026-09-23 15:04，北京时间）

已完成 5/10 题，其中 1 题通过，第六题开始运行。`t1-localvol-barrier` 在 569.64 秒、18 次模型调用后返回未完成；checker 为 **0 通过 / 7 失败 / 32 错误**，没有生成交付文件。

当前执行证据指向路径问题：脚本尝试读取 `task_dir / 'scratch/task_rules.json'`，把 Agent 的 scratch 工作区误认为只读任务根目录下的子目录，触发 FileNotFoundError。后续请求在复制文件、校验缺失输出、原样重跑之间切换，直到耗尽步骤。此次结果没有证明更高的金融任务解题能力；也表明仅按完整动作指纹计数的停滞检测无法覆盖所有跨工具循环。保留此失败，不在当前运行中修补。

### 第 4 题检查点（非最终成绩）

已完成 4/10 题，其中 1 题通过，第五题开始运行。`t1-implied-vol-approximations` 在 895.52 秒返回未完成，checker 为 **8 通过 / 2 失败 / 0 错误**，17 次模型调用尝试。未通过项均涉及 `summary.json`：生成程序计算出 NaN，严格 JSON 序列化拒绝该值并留下截断文件，运行失败门槛没有放行。

v6 同题为 0/10、外层超时；v5.6 同题为 8/10、同样未完全通过。因此本轮恢复到旧版的单项检查水平，但不是相对 v5.6 的新增成功。未来修复应追溯非有限数值的来源并按任务约定处理，不能以填零或放宽 JSON 校验代替正确计算。源码哈希、题单前缀和 starter=false 已再次核对一致，未重跑。

### 第 3 题检查点（非最终成绩）

已完成 3/10 题，其中 1 题通过，第四题开始运行。`t1-fama-french-factor-model-new` 在 895.51 秒主动返回未完成，checker 为 **0/101**，没有生成要求的 8 个交付文件；v5.6 与 v6 的同题也均未通过。运行指标记录 18 次模型调用尝试，最后一次为 ModelError，因此不应将其全部描述为已完成模型响应。

剩余问题已有明确证据：回归残差计算使用长度 2516 的收益序列与长度 2515 的设计矩阵，触发广播错误；后续缩进错误的编辑被新保护拒绝，原脚本保留，但模型仍在读取与重复失败编辑之间循环。内存检查点仍同时保留较早的编辑语法失败和当前执行的类型错误，说明本轮“同执行门槛 supersede”尚未解决跨编辑/执行门槛的陈旧修复提示。

本题证明预算收尾与语法保护生效，但金融表格对齐及循环恢复仍未解决，不能把更规范地返回失败算作任务能力提升。以上问题只记录为后续候选改进；当前实验代码保持冻结，没有针对本题插入修复或重跑。

### 第 2 题检查点（2026-09-23 14:24，北京时间）

已完成题单前 2/10 题，当前 1 题通过，第三题正在运行。`t1-earnings-surprise-calculator` 在 154.68 秒、5 次模型调用后正常完成，Agent 与 verifier 均退出 0，checker **8/8**，starter 未使用。该题在 v6 为超时失败、在 v5.6 为通过，因此本次恢复了一个 v6 丢失的通过项，但不是相对 v5.6 的新增通过。

已核对当前 2 个结果 ID 与预登记题单前缀完全一致，未重跑择优。增量 summary 暂显示 1/2=50%，它只覆盖已完成部分，**不能当作最终 10 题 pass@1**。最终指标必须等全部 10 题结束后统计。

### 首题检查点（非最终成绩）

首题 `t1-geometric-mean-reverting-jd` 已完成，第二题已启动。首题未通过，但 Agent 在约 895.32 秒主动返回未完成并保存完整运行指标，没有像 v6 一样被外层 900 秒硬终止，说明更短预算的传递在该题已生效。

| 版本 | 严格结果 | checker 通过/失败/错误 | Agent 秒数 | 已记录模型调用 |
| --- | --- | ---: | ---: | ---: |
| v5.6 同题 | checker 失败 | 22/12/0 | 374.37 | 11 |
| v6 同题 | 外层超时 | 0/0/34 | 900.15 | 17 |
| v6.1 同题 | 未完成，执行仍失败 | 20/9/5 | 895.32 | 14 |

v6.1 留下 4 个输出文件，但 `summary.json` 为空，执行失败门槛未解除；因此 reward 仍为 0。相对 v6 有更多 checker 项通过，并不等于已超越 v5.6，也不能据此宣布整体 pass@1 提升。完整 10 题结果仍待评测，未重跑首题。

### 运行环境事件：电脑休眠

2026-09-23 14:21（北京时间）检查发现：第一题进程的自然经过时间约 39 分钟，而轨迹计时约 816 秒。macOS 电源日志确认 13:52:03–14:07:48 休眠 945 秒、14:09:15–14:18:15 休眠 540 秒、14:19:00–14:19:15 休眠 15 秒，合计 1500 秒（25 分钟）。这与两个计时口径之差一致；进程恢复后轨迹继续增长，未重启或补跑。

因此本地 runner/Agent 使用的单调计时在此次环境中不计休眠；记录的 duration_sec 不等于含休眠的自然经过时间。最终报告应分别说明，不将休眠时间归因于算法或推理耗时，也不把这次运行视作完全无环境干扰的耗时对照。

14:22 左右启动仅针对评测父 PID 81698 的临时空闲休眠抑制：`caffeinate -i -t 14400 -w 81698`。父进程结束或达到 4 小时上限时自动释放；没有改永久电源配置，没有阻止屏幕熄灭，也没有使用强制阻止系统休眠的 `-s` 选项。源码、模型、题单、调用上限和评分规则均未改变。

启动时父进程 PID 81698、工具会话 3607；当时已确认子进程收到 `--time-budget-sec 900.0`，运行配置的源码哈希及题目顺序与登记一致。整个实验沿用同一进程，没有重复评测或覆盖失败记录。最终已确认父进程及附属防休眠进程均结束。

真实模型对照评测与最终效果报告已完成。新报告及修改目前仅在本地，未自动提交或推送 GitHub。
