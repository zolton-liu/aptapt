# Git 版本恢复与本地开发（2026-09-24）

本仓库已有远程 origin，但此前只有 e0ac5a5 初始提交。以下为从现存文件恢复的检查点，不是当时逐步提交的完整历史，不回填提交时间。

## 恢复范围

- v6.2：本次 CodaBench 提交时主目录的 src、scripts、tests、configs 和报告；与已发布镜像的对应关系见 submission-preparation-20260924.md。
- v6.3：work/releases/v6.3-verifier.Jgg53G 内的框架、测试、运行脚本、验证配置及经验。
- v6.4：work/releases/v6.4-evidence.L189Io 的同类文件。
- v6.5 候选：work/verifier-v6.5.DWdd1o 的同类文件；未完成模型评测，不能宣称优于旧版。

这些版本分别保存为真实 Git 提交和 checkpoint 标签。旧副本、冻结评测与压缩归档保留在原处；不把它们作为大文件重复上传。v5.6 没有可信源码快照，仍不能精确恢复。

后续开发使用独立 dev/local-verifier 分支。每个可验证改动单独提交；本地模型与正式 House 模型配置分离。任何新评测均应绑定提交、模型和配置，并保留源码快照；不得将工程测试或联通性检查标称 pass@1。

密钥、团队证明、提交 ZIP、虚拟环境、模型权重、原始任务及评测轨迹不进入 Git。公开报告仅保留不含凭据的摘要和复现信息。

## 已恢复的 Git 检查点

| 版本 | 提交 | 标签 |
|---|---|---|
| 提交比赛的 v6.2 | 6192621 | checkpoint/v6.2-submitted-20260924 |
| v6.3 冻结副本 | a8f2595 | checkpoint/v6.3-recovered-20260924 |
| v6.4 冻结副本 | e59f7c6 | checkpoint/v6.4-recovered-20260924 |
| v6.5 开发副本 | e07b30a | checkpoint/v6.5-recovered-20260924 |

恢复范围为 src/scripts/tests/configs/knowledge，主 README 和历史报告另行保留。
经验 JSON 末尾补换行，其内容未变；新实验记录新文件哈希，不能直接冒用旧经验文件字节哈希。
旧 v6.5 进度报告未记录最后的整函数编辑实现；本次已从实际文件恢复该实现及测试。

## 本轮新增

- `configs/local-dev.toml` 固定本机 qwen2.5-coder:14b 的完整 digest。仅供开发入口读取，官方 solve 配置不变。
- `scripts/dev_local.py` 提供 check / smoke / eval，拒绝远程地址、模型 digest 漂移、未提交改动及重复实验名。
- 从 Git commit 导出源码后运行，而非继续引用可变的主目录；冻结题单，沿用输入/checker 快照。提交号、逐文件哈希、实际模型、完整配置、Python 版本同时记入实验 provenance 与 run_config。
- 清除遗留回放、命令模型、House 及 QFA 开关，按本次配置重新构造运行环境；只读经验默认启用。
- AGENTS.md 记录后续提交、实验和敏感文件处理规则，避免再次只修改未提交的副本。

## 验证及局限

- 保存 v6.2 前重新测试：139 passed，25.71 秒。
- 恢复 v6.5 后重新测试：253 passed，39.29 秒。
- 新增本地运行入口与原有快照保留测试：12 passed，0.84 秒。
- 本轮合并后完整测试：261 passed，40.13 秒。
- 本地真实模型联通检查成功：1 个完成响应，40 输入 token、6 输出 token；并非回放或远程模型。检查时开发树尚未提交，因此只做联通测试，没有启动金融任务评测。
- 联通检查后的 Ollama 运行状态显示该模型实际 context_length=32768。配置输入估算预算 16000、最大输出 4000；输入估算不是精确 tokenizer 保证。
- 没有运行新十题、更新模型权重、改动旧评分或再次上传 CodaBench。当前开发版本没有新的 pass@1 结论。

## v6.6：恢复可执行性、保留验证证据

历史十题显示 v5.6 的四道成功题均使用普通可执行脚本；v6.3/v6.4 虽加入严格三层协议，
但 20 次任务 rollout 中完整阶段验收和有效 three-layer-v1 报告均为零。失败主要发生在阶段函数签名、
AuditReport 组装、输入路径和局部编辑，未到达金融交叉验证。这支持把“求解代码”和“框架验证器协议”
解耦；它不证明验证不重要，也不是单组件因果实验。

- 本地基线关闭强制阶段协议；模型写普通 solve.py，并在写最终文件前执行结构、计算和独立交叉断言。
- 框架仍在每次实际执行后检查真实产物的文件、格式、有限数值及 provenance；原严格验证器、阶段证据、
  经验晋升和实现/方法分流代码均保留，可用于单独的严格模式实验。
- 当前脚本成功且实际产物通过框架门禁时，本地基线自动完成，避免 v6 的 Asian option 已通过 25/25
  checker 却因后续 finish 循环超时计零。正式隐藏 checker 仍是最终标准。
- 提示加入历史中反复出现的通用边界规则：尊重 JSON 根类型、DataFrame 行数与 index 长度一致、
  pandas 位置访问使用 iloc；没有写入任何公开题答案或 checker 期望值。
- 修改后完整工程测试 263 passed（39.48 秒）；兼容模式对 87 个公开任务的初始上下文静态预检
  全部低于 16,000 估算 token，最高 12,011。两者都不是 pass@1。
